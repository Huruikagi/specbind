# Forward-test run: 2026-09-01 / Codex / 42724f6

[Back to the measurement dashboard](../results.md).

- Date: `2026-09-01`
- Driver: `Codex`
- Model: `gpt-5.6-terra`
- Driver profile: `medium`, fresh context with no prior turns
- Tested build: `42724f6`
- Fixture language: `ja`
- Scenarios: `R8`

## Measurements

| Scenario | Verdict | Expectation that did not hold | Fixture state left behind | Mechanical evidence | Finding |
| --- | --- | --- | --- | --- | --- |
| `R8` temp attempt | `environment_blocked` | The host refused writes outside the workspace sandbox. | No valid authored artifact. | The first write was rejected in the OS temporary fixture. | `none` |
| `R8` workspace attempt | `product_failure` | Requirements was first written under repository-root `specs/` instead of the configured Spec root. | The driver repaired the path and completed the gate, so the terminal state alone did not reveal the wrong-action risk. | The debrief identified the initial inferred path before correction. | `FT-0014` |

## Confirmation turns

The maintainer clarified the abstract cancellation boundary and approved the presented Requirements and active IDs.

## Debrief dispositions

| Scenario | Observation | Impact | Disposition | Reason or finding ID |
| --- | --- | --- | --- | --- |
| `R8` | Artifact inventory output was treated as a writable project path without target resolution. | `wrong-action-risk` | `retained` | `FT-0014` |

## Cleanup

- Fixture paths removed after the release batch was recorded.
- Main worktree retained only product and forward-test record changes.
