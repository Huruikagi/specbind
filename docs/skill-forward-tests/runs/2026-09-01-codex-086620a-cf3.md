# Forward-test run: 2026-09-01 / Codex / 086620a

[Back to the measurement dashboard](../results.md).

- Date: `2026-09-01`
- Driver: `Codex`
- Model: `gpt-5.6-terra`
- Driver profile: `medium`
- Tested build: `086620a`
- Fixture language: `en`
- Scenarios: `CF3`

## Measurements

| Scenario | Verdict | Expectation that did not hold | Fixture state left behind | Mechanical evidence | Finding |
| --- | --- | --- | --- | --- | --- |
| CF3 | `product_failure` | The base fixture established neither an external API nor independent infrastructure responsibility, but the driver added both conditional candidates instead of asking the maintainer to choose. | Modified `design-template-selection.md`; untracked `api.md` and `infrastructure.md`; existing `cart` status remained idle. | `steering list` found only `conventions` and `structure`; `template list spec` recognized both added candidates; `spec status cart` reported idle; `git diff --check` passed. | FT-0019 |

## Confirmation turns

None. The driver treated the initial request as authority for the candidate-set
change.

## Debrief dispositions

| Scenario | Observation | Impact | Disposition | Reason or finding ID |
| --- | --- | --- | --- | --- |
| CF3 | The native fixture path differed from the shell temp mapping. | `extra-step` | discarded | Fixture-path environment detail; the driver recovered without changing the measured behavior. |
| CF3 | The initial `template read` omitted the `spec` scope. | `extra-step` | discarded | CLI help supplied the accepted route. |
| CF3 | The driver inferred independent API and infrastructure responsibilities from a request for future support despite no current evidence. | `wrong-action-risk` | retained | FT-0019 |

## Cleanup

- Fixture paths removed: pending fresh confirmation.
- Main worktree after recording: contains the finding, run record, and pending fix.
