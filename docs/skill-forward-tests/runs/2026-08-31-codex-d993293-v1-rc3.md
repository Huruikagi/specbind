# Forward-test run: 2026-08-31 / Codex / d993293

[Back to the measurement dashboard](../results.md).

- Date: `2026-08-31`
- Driver: `Codex`
- Model: `not exposed by the driver session`
- Driver profile: `session default; fresh-context subagents`
- Tested build: `d993293`
- Fixture language: `en`
- Scenarios: `S5, X1, RT1, CLI recovery, T1`

## Measurements

| Scenario | Verdict | Expectation that did not hold | Fixture state left behind | Mechanical evidence | Finding |
| --- | --- | --- | --- | --- | --- |
| `S5` | `pass` | none | New `.specbind/steering/testing.md`; existing Steering preserved | `steering list` and reads succeeded; the new document used `artifact_id: testing`, the project path, and the maintain instruction; debrief exposed a milestone-status wording conflict | `FT-0008` |
| `X1` | `product_failure` | The recovery report did not name Design as owner, state the full rewind cost, or give the exact invalidate command | Contract Review remained unaccepted; no managed state write | Fresh review detected the invariant mismatch, but its terminal report omitted `specbind spec design invalidate cart` and the Design-owned rewind boundary | `FT-0002` |
| `RT1` base rejection | `pass` | none | Only the received `src/cart.py` implementation diff remained | Review returned `REJECTED` for the off-by-one upper bound and made no repository write | none |
| `CLI recovery` | `pass` | none | No repository change | `specbind milestone stats` returned only the unknown-subcommand error and nested usage; the fresh driver recovered with `specbind milestone status` and received `OK MILESTONE_STATUS_REPORTED` | `FT-0005` |
| `T1` temporary-path attempt | `environment_blocked` | The fixture could not be mutated under the host temporary path | Draft boundary not reached | The driver could read the installed Skill but the host write sandbox denied the required artifact write | none |
| `T1` workspace retry | `pass` | none | One approved pending Task covering Requirements 1.1-1.4; implementation not started; clean fixture | Draft contained behavior and automated verification in the same Task, stopped for approval, then `tasks approve` advanced the Spec to implementation | `FT-0006` |

## Confirmation turns

T1 received approval only after presenting the complete one-Task draft. The
temporary-path attempt received no approval because the environment stopped it
before the draft boundary.

## Debrief dispositions

| Scenario | Observation | Impact | Disposition | Reason or finding ID |
| --- | --- | --- | --- | --- |
| `S5` | The Skill prohibited milestone-state reads while also requiring a completion-safety status preflight | `ambiguity` | `retained` | `FT-0008` |
| `CLI recovery` | The error did not print the correct replacement command | `ambiguity` | `discarded` | Help/usage remained accurate and project instructions supplied the supported command; no unrelated suggestion appeared |
| `T1` | Approval and active Task IDs remained guarded | `extra-step` | `discarded` | Intended authority and subject-resolution boundaries |

## Cleanup

- Fixture paths removed: `none; ignored fixtures retained until the release batch is recorded`
- Main worktree after recording: run-record changes only
