# Forward-test run: 2026-09-01 / Codex / 8db72c0

[Back to the measurement dashboard](../results.md).

- Date: `2026-09-01`
- Driver: `Codex`
- Model: `gpt-5.6-terra`
- Driver profile: `medium`
- Tested build: `8db72c0`
- Fixture language: `en`
- Scenarios: `DS9`

## Measurements

| Scenario | Verdict | Expectation that did not hold | Fixture state left behind | Mechanical evidence | Finding |
| --- | --- | --- | --- | --- | --- |
| DS9 | `product_failure` | The driver created only `design/main` and `contract.yaml` instead of proposing an infrastructure supplement before writing. | Unapproved `design.md` and `contract.yaml`. | `check traceability infrastructure` passed; `check contracts` passed with an existing cart warning; `git diff --check` passed. | FT-0020 |

## Confirmation turns

The driver stopped only for Design approval, after writing the ordinary Design
and Contract.

## Debrief dispositions

Pending after the fresh confirmation; this first result is sufficient to locate
the missed decision point.

## Cleanup

- Fixture path removed before fresh confirmation: pending.
