# Forward-test run: 2026-09-03 / Codex / 3a7b96a

[Back to the measurement dashboard](../results.md).

- Date: `2026-09-03`
- Driver: `Codex`
- Model: `gpt-5.6-terra`
- Driver profile: `medium`
- Tested build: `3a7b96a`
- Fixture language: `en`
- Scenarios: `A4`

## Measurements

| Scenario | Verdict | Expectation that did not hold | Fixture state left behind | Mechanical evidence | Finding |
| --- | --- | --- | --- | --- | --- |
| `A4` | `scenario_invalid` | The first recipe described a nonexistent cart read API and exposed an unconsumed Contract entry, so a clean Contract Review required unsupported inference. | Clean at `c1315bb`; one reverse Spec remained `adoption_ready` with fresh review and finalization actionable. | Status projected `adoption_finalize` to `sb-discovery/reverse_resume`; source diff was empty; finalization was blocked by `ENV-0004`. | `FT-0043` |

## Confirmation turns

The driver accepted Contract Review, then the host rejected reverse finalization
even after the driving session relayed explicit approval.

## Debrief dispositions

| Scenario | Observation | Impact | Disposition | Reason or finding ID |
| --- | --- | --- | --- | --- |
| `A4` | The reverse procedure did not expose the strict `log_entries` JSON shape, so the driver read implementation source to infer it. | `ambiguity` | `retained` | `FT-0043` |
| `A4` | The fixture's unconsumed export forced an unsupported external-consumer interpretation. | `wrong-action-risk` | `discarded` | Invalid first A4 recipe. |

## Cleanup

- Fixture paths removed: `/tmp/sb-a4-0192`
- Main worktree after recording: run-record changes only
