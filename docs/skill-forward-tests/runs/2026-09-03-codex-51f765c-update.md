# Forward-test run: 2026-09-03 / Codex / 51f765c

[Back to the measurement dashboard](../results.md).

- Date: `2026-09-03`
- Driver: Codex
- Model: `gpt-5.6-terra`
- Driver profile: `medium`, fresh context with no prior turns
- Tested build: `51f765c`
- Fixture language: `en`
- Scenarios: `U1`, `U2`, `U3`

## Measurements

| Scenario | Verdict | Expectation that did not hold | Fixture state left behind | Mechanical evidence | Finding |
| --- | --- | --- | --- | --- | --- |
| `U1` | `product_failure` | The natural request did not discover `sb-configure` or prove mise ownership; it ran only the project-asset install path. | HEAD unchanged; clean worktree; no project mise configuration. | Driver command record contained `specbind install --help`, dry run, and apply but no Skill, language, configuration, or mise ownership read. Fixture `git status --short` was empty at the original install commit. | `FT-0025` |
| `U2` | `pass` | `none` | Simulated version `1.3.0`; `latest` retained; clean worktree after two local commits. | First commit `3b1d812` contained only `mise.lock`; second `dbd3f68` contained only both installed update references. Both old-package markers were absent, exact fixture `specbind --version` returned `1.3.0`, and final dry run reported 108 keep actions with no create, replace, or remove. | `FT-0026` from debrief |
| `U3` | `pass` | `none` | Exact `1.2.0` selector, lock, simulated version, old-package marker, and HEAD retained; clean worktree. | Direct reads showed unchanged `mise.toml`, `mise.lock`, `.forward-test/specbind-version`, and installed marker at setup commit `31567eb`. | `none` |

## Confirmation turns

None. U1 stopped at no proved project owner, U2 had complete explicit update
authority, and U3 stopped before mutation because no exact target was supplied.

## Debrief dispositions

| Scenario | Observation | Impact | Disposition | Reason or finding ID |
| --- | --- | --- | --- | --- |
| `U1` | “Latest supported version” was interpreted as refreshing assets with the PATH-selected binary because project instructions did not route the natural update request. | `ambiguity` | `retained` | `FT-0025` |
| `U2` | The update reference required two checkpoints while the general Git adapter grouped `sb-configure` plus aftercare into one. | `ambiguity` | `retained` | `FT-0026` |
| `U3` | The driver read aftercare before any mutation even though it applies after mutation. | `extra-step` | `discarded` | It did not affect the exact-pin stop or mutate the fixture. |

All three debriefs left their already judged fixtures unchanged.

## Cleanup

- Fixture paths removed: pending after the confirmation run
- Main worktree after recording: routing and adapter fixes plus this run record,
  dashboard, and worklist updates
