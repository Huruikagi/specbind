# Forward-test run: 2026-09-01 / Codex / 035f093

[Back to the measurement dashboard](../results.md).

- Date: `2026-09-01`
- Driver: `Codex`
- Model: `gpt-5.6-terra`
- Driver profile: `medium`, fresh-context subagent with no prior turns
- Tested build: `035f093`
- Fixture language: `en`
- Scenarios: `DR1`

## Measurements

| Scenario | Verdict | Expectation that did not hold | Fixture state left behind | Mechanical evidence | Finding |
| --- | --- | --- | --- | --- | --- |
| DR1 | `pass` | none | Active Direct-only milestone at `implementation`; `contributing-guide` completed; `cart-contract-change` pending; clean worktree | `milestone status --json` reported `1/2` Direct items complete and only `cart-contract-change` actionable; `git log` showed separate `7fe23c1` implementation and `fe4dfd4` Roadmap-state commits; `CONTRIBUTING.md` existed; cart Requirements contained no cap; no release archive existed | none |

The driver discovered the installed milestone workflow without being told its
name. It parked the invalid Direct classification, continued the independent
guide item through its ordinary implementation and completion handshakes, then
reported the Discovery reroute after reachable work was exhausted. It did not
ask for a confirmation turn or run Release.

## Confirmation turns

None. DR1 intentionally measures whether a human decision can be accumulated
without interrupting independent reachable work.

## Debrief dispositions

The fixture was clean before and after the read-only debrief.

| Scenario | Observation | Impact | Disposition | Reason or finding ID |
| --- | --- | --- | --- | --- |
| DR1 | The canonical-Requirements wording conflicted with the Direct boundary. | wrong-action-risk | discarded | This is the scenario's deliberate invalid-Direct trigger; the Skill selected the required safe reroute. |
| DR1 | The driver tried `specbind artifact list` without its required Spec operand before returning to milestone and repository reads. | extra-step | discarded | One harmless invalid probe; the CLI diagnostic was exact and no wrong action or recurring ambiguity was observed. |

## Cleanup

- Fixture paths removed: `/tmp/sb-dr1-035f093`
- Main worktree after recording: only this run record and dashboard projection
  were modified
