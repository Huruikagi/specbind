# Forward-test run: 2026-08-31 / Codex / 8aaa198

[Back to the measurement dashboard](../results.md).

- Date: `2026-08-31`
- Driver: `Codex`
- Model: `not exposed by the driver session`
- Driver profile: `session default; fresh-context subagents`
- Tested build: `8aaa198`
- Fixture language: `en`
- Scenarios: `S5, RT1, HP1`

## Measurements

| Scenario | Verdict | Expectation that did not hold | Fixture state left behind | Mechanical evidence | Finding |
| --- | --- | --- | --- | --- | --- |
| `S5` ambiguous recovery | `pass` | none | Only the later duplicate `conventions-copy.md` was removed; list/read healthy | Git history proved `conventions.md` came from the initial install and `conventions-copy.md` from a later commit before deletion; the fixture-local CLI then listed exactly two Steering documents | `FT-0007` |
| `RT1` deferred branch | `pass` | none | Received `src/cart.py` diff plus one new `.specbind/deferred.md`; Task still pending | The correct implementation was approved under byte-identical pre/post evidence status; only after verdict did the active adapter receive one deferred naming cleanup | `FT-0004` |
| `HP1` | `pass` | none | Final commit `00f2235`; tagged implementation commit `bcb9f39`; no active milestone; `cart` idle; clean fixture | Judge reported all expectations PASS, including both release archives, annotated `v1.4.0`, tag before finalization, no remote, and 10 dispatch contexts | none |

## Confirmation turns

RT1 did not mutate before its verdict. HP1 preserved separate confirmations for
Discovery scope, delegated Requirements/Design/Tasks gates, completion
acceptance, and Publish. The driver also stopped after Tasks approval,
implementation, the expected not-ready preflight, and the final Publish
proposal exactly as the journey requires.

## Debrief dispositions

| Scenario | Observation | Impact | Disposition | Reason or finding ID |
| --- | --- | --- | --- | --- |
| `S5` | Git history is an extra read needed to identify the newly introduced duplicate | `extra-step` | `discarded` | Intended fail-closed recovery evidence |
| `RT1` | The prose required initial status first but the command example listed diff first | `ambiguity` | `retained` | `FT-0009` |
| `HP1` | After 1/1 Tasks completed, `spec status` still reported `Next action: implementation` while milestone status reported validation | `wrong-action-risk` | `retained` | `FT-0010` |
| `HP1` | Registered roles were unavailable as direct collaboration selectors | `ambiguity` | `discarded` | The installed Skills explicitly permit ordinary fresh subagents; 10 dispatch contexts proved orchestration occurred |
| `HP1` | Requirement 3.2's delivery wording could be read as requiring publication before completion validation | `ambiguity` | `discarded` | Design and Tasks explicitly assigned binding/publication to the later release lifecycle, and the exact journey passed |
| `HP1` | Windows PATH, sandbox retry, exact-command evidence, and detached worktree safety added steps | `extra-step` | `discarded` | Environment and release-safety workarounds preserved the canonical command and product conclusion |

## Cleanup

- Fixture paths removed: `none; ignored fixtures retained until the release batch is recorded`
- Main worktree after recording: run-record changes only
