---
type: SpecBind Brief
---

<!-- specbind:instruction create bind=spec
Resolve `spec` to the canonical Spec identity in the current authoring context.
Replace every `{{spec}}` reference with that exact value and keep it in the
title so the artifact remains identifiable when read outside its directory.
-->

# `{{spec}}` Brief

<!-- specbind:instruction maintain
Capture the requested change in the requester's own terms. Keep it short and
fold later requests about the same change into this one brief.
-->

<!-- specbind:instruction consume
Treat this as request context, not authoritative scope. Requirements owns that
scope, and this document is not fingerprinted.
-->

## Problem

<!-- specbind:instruction maintain
State the problem, current difficulty, or opportunity the requester wants to
address in the terms available at request time. Do not anticipate a solution or
implementation method here.
-->

## Desired outcome

<!-- specbind:instruction maintain
State the result by which the requester would recognize success. This is request
context before elaboration into acceptance criteria, not a complete behavioral
contract.
-->

## Requested boundaries

<!-- specbind:instruction maintain
Record what the requester explicitly included, excluded, or allowed to change.
Do not import scope later established by Discovery or Requirements. Delete this
section when the desired outcome already carries every known boundary.
-->

## Assumptions and dependencies

<!-- specbind:instruction maintain
Record request-time assumptions, other changes, external decisions, deadlines,
or dependencies that affect how this request is handled. Delete this section
when none apply.
-->

## Source material

<!-- specbind:instruction maintain
For material supplied by the requester or an explicit Source Collection used by
Discovery, keep only the exact URL or project-relative path relevant to this
Spec and state why each item matters. Keep the complete collection disposition
and cross-Spec mapping in the Roadmap. Delete this section when there is no
source material.
-->
