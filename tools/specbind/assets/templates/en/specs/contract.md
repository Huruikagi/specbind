---
type: SpecBind Contract
---

# Contract

<!-- specbind:instruction maintain
This document is the durable seam other Specs depend on, not a design summary.
The five section headings below are machine-read and must keep their exact names
and order. IDs are stable lowercase kebab-case tokens in inline code.

Use these exact entry shapes:
- Owns, Exports, Invariants: - `id` — description
- Consumes: - `local-id` → `spec/section/target-id` — optional description
- File Ownership: - `id` — `project/relative/path`, `another/path/**`
Additional ownership paths use a comma and one space. File Ownership is a sparse
list of persistent cross-Spec boundaries, not an inventory of every implementation
file.

Leave a section empty when it genuinely has no entries.
-->

## Owns

## Exports

## Consumes

## Invariants

## File Ownership
