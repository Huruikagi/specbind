---
type: SpecBind Design
artifact_id: ui
---

<!-- specbind:instruction create output=spec
現在のauthoring contextにある正規のSpec identityから`spec`を生成する。
すべての`{{spec}}`参照をその同じ出力で置換し、ディレクトリ外で読んでも対象を
識別できるようタイトルに残す。
-->

<!-- specbind:instruction create output=artifact_id
このテンプレートのFront Matterにあるリテラルなcollection identityから`artifact_id`を
生成し、すべての`{{artifact_id}}`参照をその同じ出力で置換する。
-->

# `{{spec}}` の設計 — `{{artifact_id}}`

<!-- specbind:instruction maintain
プロジェクトの選択ルールが、このSpecの現在の責任にこの文書を適用すると判定する間だけ
保持する。この文書が扱うすべてのRequirementをFront Matterの`requirement_ids`配列に
列挙し、それを実現する判断の近くに同じIDを厳密な`_Requirements: 1.1, 1.2_`形式で記載する。

ピクセル単位の完成図ではなく、ユーザーに見える振る舞いと状態を設計する。該当しない節は
削除する。読者と保守担当者が独立して追う永続的なUI責任がある場合にだけ、別のDesign
identityへ分割する。
-->

## ユーザーと利用状況

<!-- specbind:instruction maintain
このUIを使う利用者、利用する目的、端末や場所など振る舞いに影響する状況を記載する。
一般的なペルソナ説明ではなく、このSpecの画面判断に必要な差だけを残す。
-->

## デザインシステムの適用

<!-- specbind:instruction maintain
Steeringやプロジェクトの実装資産が定めるデザインシステムのうち、このSpecが適用する原則、
トークン、コンポーネントを参照する。正本の内容は複製せず、このSpecでの適用判断、追加、
意図的な例外と理由だけを記載する。該当する共通基盤がなく、このSpecだけの判断もなければ
この節を削除する。複数のSpecが今後も継承すべき新しい原則は、この文書だけに閉じない。
-->

## 画面一覧

<!-- specbind:instruction maintain
このSpecが新設または変更する画面を一覧にする。文書内で一貫した画面IDを付け、画面名、
利用者にとっての目的を表す一行の要約、主な利用者、主な進入元を記載する。項目や状態の詳細は
各画面の「画面設計」に置き、ここでは重複させない。
-->

| 画面ID | 画面名 | 目的 | 主な利用者 | 主な進入元 |
| --- | --- | --- | --- | --- |

## ナビゲーションと操作フロー

<!-- specbind:instruction maintain
画面間の遷移と、戻る、キャンセル、再開など複数画面にまたがる操作を記載する。単一画面内で
完結する操作は、その画面の「操作と結果」に記載する。関係を文章や表だけでは誤読しやすい
場合にだけ、遷移図を使用する。画面をまたぐフローがない場合はこの節を削除する。
-->

## 画面設計

<!-- specbind:instruction maintain
画面一覧にある画面ごとに、次の小節一式を繰り返す。

- 「目的と表示条件」には、利用者が達成すること、権限、前提状態を記載する。
- 「表示項目」には、利用者の判断、入力、権限、状態表示に影響する項目を記載する。見た目だけの
  ラベルやピクセル単位の配置は列挙せず、同じ構造の反復項目はまとめる。
- 「操作と結果」には、利用者が開始する操作ごとの実行条件、成功時の結果または遷移先、失敗時の
  フィードバックを記載する。画面をまたぐ共通フローは「ナビゲーションと操作フロー」を参照し、
  操作のない画面ではこの小節を削除する。
- 「状態と入力フィードバック」には、該当する読み込み中、空、エラー、利用不能状態と、入力検証の
  タイミング、表示箇所、修正後の回復を記載する。固有の状態や入力がない画面ではこの小節を削除する。
- 「レイアウトと情報階層」には、主要領域の位置関係、情報と操作の優先順位、画面幅によって構成が
  変わる箇所を記載する。既存のデザインシステムをそのまま適用でき、画面固有の判断がない場合は
  この小節を削除する。文章や表では空間関係を誤読しやすい場合にだけ簡易なASCIIワイヤーフレームを
  使用し、ピクセル寸法、色、装飾の完成図として扱わない。

画面数だけを理由にDesign identityを分割しない。別の担当者が独立して保守し、別々に変化する
永続的なUI責任がある場合にだけ分割を検討する。
-->

<!-- specbind:instruction create
`<画面ID> <画面名>`を画面一覧の実際の値に置き換え、画面ごとに小節一式を繰り返す。
空の例示見出しはlive artifactに残さない。
-->

### `<画面ID>` `<画面名>`

#### 目的と表示条件

#### 表示項目

| 項目 | 内容またはデータ元 | 表示・編集条件 |
| --- | --- | --- |

#### 操作と結果

| 操作 | 実行条件 | 結果または遷移先 | 失敗時のフィードバック |
| --- | --- | --- | --- |

#### 状態と入力フィードバック

#### レイアウトと情報階層

## レスポンシブ動作

<!-- specbind:instruction maintain
画面間で共通するブレークポイントの考え方、入力方式の違い、表示・非表示ではなく再配置で
守る情報の優先順位を記載する。画面固有の構成変更は各画面の「レイアウトと情報階層」に置く。
デザインシステムの共通方針から追加または変更する判断がない場合はこの節を削除する。
-->

## アクセシビリティ

<!-- specbind:instruction maintain
キーボード操作、フォーカス順、読み上げ、コントラスト、動きの低減など、このSpecで保証する
利用方法を記載する。デザインシステムが定める共通基準は複製せず、適用上の判断と例外を示す。
追加または変更する保証がない場合はこの節を削除する。
-->

## コンポーネント、データ、サービスの境界

<!-- specbind:instruction maintain
画面設計を実現するUIコンポーネントの責任、表示データの所有元、更新や副作用を依頼する
サービス境界を記載する。画面項目やContractの接合面一覧は複製しない。`design/main`が必要な
境界を十分に所有し、この文書にUI固有の判断がない場合はこの節を削除する。
-->

## UI検証方針

<!-- specbind:instruction maintain
主要な操作フロー、状態、入力フィードバック、レスポンシブ動作、アクセシビリティをどの層で
検証するかを記載する。実装手段の一覧ではなく、このUIの失敗を検出できる観測可能な結果を示す。
-->
