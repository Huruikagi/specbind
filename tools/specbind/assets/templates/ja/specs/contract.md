---
type: SpecBind Contract
---

# Contract

<!-- specbind:instruction maintain
この文書は他の Spec が依存する永続的な接合面であり、設計の要約ではない。
以下の 5 つの節見出しは機械可読であり、名前と順序を厳密に保つ必要がある。日本語化しない。
ID はインラインコードで囲んだ、安定した小文字ケバブケースとする。

次の項目形式を厳密に使う。
- Owns、Exports、Invariants: - `id` — 説明
- Consumes: - `local-id` → `spec/section/target-id` — 任意の説明
- File Ownership: - `id` — `project/relative/path`, `another/path/**`
所有パスを追加する場合はカンマと半角スペースで区切る。File Ownership は Spec 間の永続的な
境界を疎に列挙するもので、すべての実装ファイルの一覧ではない。

エントリが本当に存在しない節は空のままにする。
-->

## Owns

## Exports

## Consumes

## Invariants

## File Ownership
